# Normalize all text files to LF in the repo and in working trees.
# .editorconfig already enforces LF in editors; this enforces it in git.
* text=auto eol=lf

# Lockfiles + generated files
pnpm-lock.yaml text eol=lf
